query GetSkills($workerId: String!) {
  getSkills(workerId: $workerId) {
    id
    workerId
    worker
    status
    rating
    ratingsNumber
    category
    title
    description
    pricing
    duration
    zone
    pictures
    workerPicUrl
  }
}
